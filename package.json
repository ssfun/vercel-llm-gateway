{
  "name": "llm-gateway",
  "version": "1.0.0",
  "description": "Hybrid API Gateway with Edge and Background processing",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build required for Edge Functions'",
    "deploy": "vercel --prod",
    "lint": "tsc --noEmit",
    "format": "prettier --write \"**/*.{ts,json,md}\""
  },
  "dependencies": {
    "@vercel/node": "^3.0.11"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2",
    "vercel": "latest"
  },
  "engines": {
    "node": ">=18"
  }
}
