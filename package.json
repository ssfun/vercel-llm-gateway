{
  "name": "llm-gateway",
  "version": "1.0.0",
  "description": "Hybrid API Gateway with Edge and Background processing",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Build completed'",
    "deploy": "vercel --prod",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@vercel/node": "^3.0.11"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2"
  },
  "engines": {
    "node": "20.x"
  }
}
